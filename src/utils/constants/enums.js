export  const roles = {
    USER:"user",
    ADMIN:"admin",
    SELLER:"seller"
}
Object.freeze(roles)


//status 
export const status={
    PENDING:"pending",
    VERIFIED:"verified",
    BLOCKED:"blocked"
}
Object.freeze(status)
export const coupunStatus ={
    FIXEDAMOUNT:'fixedAmount',
    PRECENTAGE:'precentage'
}
Object.freeze(coupunStatus)

export const paymentMethods ={
    CASH:"cash",
    VISA:"visa"
}

Object.freeze(paymentMethods)
